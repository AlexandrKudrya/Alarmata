# data

Data layer containing data source implementations and repository implementations.

## Subfolders
- `local/` — Local data sources using Room database (DAOs and entities).
- `remote/` — Remote data sources using Retrofit (API interfaces, DTOs, interceptors).
- `repository/` — Repository interface implementations coordinating local and remote data sources.
