# domain

Domain layer containing pure Kotlin business logic with no Android dependencies.

## Subfolders
- `models/` — Domain model classes representing core business entities.
- `repositories/` — Repository interface definitions (contracts for data layer).
- `usecases/` — Use cases encapsulating business logic operations, organized by feature.
