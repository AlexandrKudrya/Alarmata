# features.tasks

Task engine and task type implementations.

## Subfolders
- `engine/` — Core task interfaces and factory (Task, TaskFactory, TaskProgress).
- `implementations/` — Concrete task implementations (MathTask, QRCodeTask, ShakeTask, MemoryTask, TypingTask, AIQuestionTask).
